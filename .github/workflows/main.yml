name: First Workflow

on:
    push:
      branches:
        -main

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting massage
      run: echo "welcome to our first Github actions workflow"
